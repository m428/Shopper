<h1>All Groceries</h1>

<ul id="posts">
  <% @posts.each do |post| %>
    <li>
      <%= link_to post.title, post_path(post), class: "post_title", id: "post_#{post.id}" %>
    </li>
  <% end %>
</ul>


<%= link_to "Add Grocery Item", new_post_path, class: "new_post" %>
